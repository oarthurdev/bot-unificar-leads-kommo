# ===== Conta Kommo =====
# SubdomÃ­nio da conta: https://SEUSUBDOMINIO.kommo.com
KOMMO_SUBDOMAIN=seusubdominio
KOMMO_DOMAIN=kommo.com

# (Opcional) URL customizada da lista de leads (ex.: jÃ¡ filtrada por pipeline/etapa).
# Se vazio, usa https://SUBDOMINIO.DOMINIO/leads/list/
CUSTOM_LIST_URL=

# ===== Comportamento =====
# true = navegador invisÃ­vel (mais rÃ¡pido). Use false na primeira vez para calibrar.
HEADLESS=true

# true = simula tudo mas NÃƒO confirma a unificaÃ§Ã£o (recomendado no primeiro teste)
DRY_RUN=true

# Quantos GRUPOS de duplicados unificar por execuÃ§Ã£o (0 = todos de uma vez)
BATCH_SIZE=50

# MÃ¡ximo de leads selecionados numa Ãºnica unificaÃ§Ã£o (grupos maiores sÃ£o divididos
# em rodadas, sempre mantendo o lead mais recente em todas)
MAX_LEADS_POR_UNIFICACAO=6

# Pausa entre grupos, em ms (evita rate-limit / sobrecarga da UI)
PAUSA_ENTRE_GRUPOS_MS=800

# Timeout padrÃ£o de espera de elementos, em ms
TIMEOUT_MS=20000

# Desacelerar aÃ§Ãµes para depuraÃ§Ã£o (ms entre aÃ§Ãµes; 0 = velocidade mÃ¡xima)
SLOW_MO=0

# ===== Destino dos leads unificados =====
PIPELINE_DESTINO=12347316
STATUS_DESTINO=

